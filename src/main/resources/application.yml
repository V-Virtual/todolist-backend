server:
    port: 8080

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/db_todolist?useUnicode=true&characterEncoding=utf8
        username: root
        password: duotui123@ZGH
    jpa:
        hibernate:
            ddl-auto: validate
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true
    flyway:
        baseline-on-migrate: true
